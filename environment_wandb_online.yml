# 仅创建 Python 3.10 + pip；其余依赖请：
#   pip install torch ...（按 CUDA）
#   pip install -r requirements-train-online.txt
#
name: snowflake-wandb
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
